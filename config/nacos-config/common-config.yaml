spring:
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: nacos:8848
        namespace: dev
        group: charging-platform
      config:
        enabled: true
        server-addr: nacos:8848
        file-extension: yaml
        namespace: dev
        group: charging-platform
        refresh-enabled: true
